@* @model DemoMVC.Models.SalaryModel

<h2>Nhập thông tin lương</h2>

<form asp-action="Index" method="post">
    <div class="form-group">
        <label asp-for="FullName">Họ tên:</label>
        <input asp-for="FullName" class="form-control" />
        <span asp-validation-for="FullName" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="BasicSalary">Lương cơ bản:</label>
        <input asp-for="BasicSalary" class="form-control" />
        <span asp-validation-for="BasicSalary" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="SalaryCoefficient">Hệ số lương:</label>
        <input asp-for="SalaryCoefficient" class="form-control" />
        <span asp-validation-for="SalaryCoefficient" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="Allowance">Phụ cấp:</label>
        <input asp-for="Allowance" class="form-control" />
        <span asp-validation-for="Allowance" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Tính lương</button>
</form>

@if (Model != null && Model.CalculatedSalary.HasValue)
{
    <h2>Kết quả tính lương</h2>
    <p>Họ tên: @Model.FullName</p>
    <p>Lương cơ bản: @Model.BasicSalary</p>
    <p>Hệ số lương: @Model.SalaryCoefficient</p>
    <p>Phụ cấp: @Model.Allowance</p>
    <p>Lương tính toán: @Model.CalculatedSalary</p>
} *@
